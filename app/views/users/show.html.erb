<div class="main user-show">
  <div class="container">
    <div class="user-info">
      <img src="<%= "/user_images/#{user.image_name}" %>"　align="left">
      <p>
        <strong>Uesr_ID:</strong>
        <%= @user.user_id %>
      </p>
      <p>
        <strong>Email:</strong>
        <%= @user.email %>
      </p>
      <div class="user-menu">
        <%= link_to("編集", "/users/#{@user.id}/edit") %>
      </div>
    </div>
  </div>
</div>